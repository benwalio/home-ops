apiVersion: v1
kind: Service
metadata:
  name: qbittorrent
  namespace: external-services
spec:
  # selector:
  #   app.kubernetes.io/name: qbittorrent
  ports:
    - name: http
      # protocol: HTTP
      port: 8080
      # targetPort:
  type: ClusterIP
